language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t aidario/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true aidario/docker-react npm run test
